spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://${testDbIp:localhost}:3306/photos_map_test?useUnicode=true&characterEncoding=utf8&autoReconnect=true&serverTimezone=Asia/Seoul
    username: ${testDbId:user1}
    password: ${testDbPw:1234}
  jpa:
    hibernate:
      ddl-auto: create